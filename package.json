{
  "name": "phone-otp",
  "module": "src/index.ts",
  "dependencies": {
    "zod": "^4.1.12"
  },
  "private": true,
  "scripts": {
    "dev:server": "bun run dev --workspace=server",
    "dev:extension": "bun run dev --workspace=extension",
    "build:extension": "bun run build --workspace=extension",
    "test": "bun test --timeout=120000 --coverage"
  },
  "type": "module",
  "workspaces": [
    "server/*",
    "extension/*"
  ]
}