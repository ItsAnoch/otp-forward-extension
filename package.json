{
  "name": "phone-otp",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "server/*",
    "client/*"
  ],
  "scripts": {
    "dev:server": "bun run server --workspace=server",
    "dev:client": "bun run client --workspace=client",
    "dev": "concurrently \"bun run dev:server\" \"bun run dev:client\""
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}