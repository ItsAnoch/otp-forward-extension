{
  "name": "server",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "NODE_ENV=development bun --hot --env-file=../.env run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --env-file=../.env"
  },
  "dependencies": {
    "hono": "^4.10.4",
    "zod": "^4.1.12"
  }
}
